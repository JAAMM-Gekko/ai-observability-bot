fastapi
uvicorn[standard]
python-dotenv
jinja2
beeai-framework
chromadb
pandas
sentence-transformers
openpyxl

# OpenTelemetry
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http

# AI / LLM
openai>=1.0.0

# Optional observability packages — both are wrapped in try/except in agent.py and main.py,
# so the app starts fine without them. Install manually if your platform supports it:
#   pip install openlit openinference-instrumentation-beeai
